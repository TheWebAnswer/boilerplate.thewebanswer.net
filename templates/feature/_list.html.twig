<ul class="mt-5">
    {% for feature in features %}
    <li class="flex flex-row justify-between items-center py-2">
        {% if feature.isDone %}
            <span class="text-green-500">
                <i class="fa-solid fa-square-check"></i>
            </span>

        {% else %}
            <span class="text-red-500">
                 <i class="fa-solid fa-square-xmark"></i>
            </span>
        {% endif %}
        <h3>{{ feature.title }}</h3>
        {% include 'components/button/outline/_btn-outline-primary.html.twig' with {
            srOnly: "Modifier",
            path: path('app_feature_edit', {id: feature.id}),
            icon: "pen-to-square"
        } %}
        {% include 'feature/_delete_form.html.twig' %}
    </li>
    {% endfor %}
</ul>