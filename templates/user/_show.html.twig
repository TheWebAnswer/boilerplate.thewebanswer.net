<section class="flex flex-col text-center">
    <h3 class="text-4xl font-montserrat">{{ user.firstName }} {{ user.lastName }}</h3>
    <p>{{ user.email }}</p>
    <p class="text-sm font-semibold text-gray-500">Date de création
        : {{ user.createdAt|format_datetime('full', 'none', locale='fr') }}</p>
    <p class="text-sm font-semibold text-gray-500">Date de modification
        : {{ user.updatedAt|format_datetime('full', 'none', locale='fr') }}</p>
</section>